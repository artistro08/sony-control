<UserControl
    x:Class="SonyControl.App.Views.Settings.HeadsetSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:SonyControl.App.Views">

    <!-- Headset Picker -->
    <StackPanel Margin="0,0,0,16" Spacing="8">
        <ComboBox
            MinWidth="240"
            AutomationProperties.Name="Headphones"
            DisplayMemberPath="DeviceName"
            Header="Headphones"
            ItemsSource="{x:Bind ViewModel.Headsets, Mode=OneWay}"
            SelectedIndex="{x:Bind ViewModel.SelectedHeadsetIndex, Mode=TwoWay}"
            Visibility="{x:Bind ViewModel.HasHeadset, Mode=OneWay}" />
        <InfoBar
            IsClosable="False"
            IsOpen="{x:Bind ViewModel.NoHeadset, Mode=OneWay}"
            Message="Pair your Sony headphones in Bluetooth settings to change these settings."
            Severity="Informational"
            Visibility="{x:Bind ViewModel.NoHeadset, Mode=OneWay}" />

        <!-- Disconnected: the settings below are disabled until it reconnects. Collapsed when
             closed too, or the panel still spaces around it. -->
        <views:DisconnectedBar Headset="{x:Bind ViewModel.SelectedHeadset, Mode=OneWay}" Visibility="{x:Bind ViewModel.SelectedHeadset.ShowDisconnectedWarning, Mode=OneWay, FallbackValue=Collapsed}" />
    </StackPanel>
</UserControl>
